{
  "apps": [
    {
      "appId": "02c98b92",
      "root": ".",
      "dependencyInstallCommand": "npm install && npm install @rollup/rollup-darwin-arm64 && npm run build:web && npm run generate-assets && npx cap sync"
    }
  ]
}
