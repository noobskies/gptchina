{
  "apps": [
    {
      "appId": "02c98b92",
      "root": "client",
      "dependencyInstallCommand": "cd .. && npm install && npm run frontend",
      "webBuildCommand": "find dist -name '*.gz' -delete 2>/dev/null || true && npx cap sync android && mkdir -p ../android && cp -R android/* ../android/"
    }
  ]
}
